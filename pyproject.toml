[project]
name = "cig-scraper"
version = "0.1.0"
description = "Download the Cigarette Pack NFT images from IPFS into Cloudflare R2 with Modal"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "modal>=1.3.0.post1",
    "requests>=2.32.5",
]
